- Fix appearance of a user that does not have a photo

- Publish the deleted technologies but only with minimal data, not all data.
- Add index to Technologies.name
- Add index to Meteor.users.profile.name

- Take care of XSS when inserting HTML to the contribution
- Sanitize H1s etx when adding a contribution

Create an about page
    rantav
    thank:
        meteor
        alertify

- Add support for Yassib or just borrow the concepts from it http://www.yasiv.com/github/#/costars?q=meteor%2Fmeteor
- Make technology search case insensitive, as in /technology/java
- Add typing smartness: /github/user/project, /technology/name, /contributor/name with autocomplete and nice display